name: Nightly APK

on:
  push:
    branches: [master]
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch: {}

concurrency:
  group: nightly-build
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - uses: actions/setup-java@v6.0.1
        with:
          distribution: temurin
          java-version: 17

      - name: Merge upstream
        id: merge
        run: |
          git config user.email "actions@github.com"
          git config user.name "GitHub Actions"
          git remote add upstream https://github.com/coredevices/mobileapp.git
          git fetch upstream
          before=$(git rev-parse HEAD)
          git merge --no-ff upstream/master --no-edit
          if ! git diff --quiet "$before" HEAD; then
            git checkout HEAD^ -- .github/workflows
            git add -A .github/workflows
            git commit --amend --no-edit --no-verify || true
          fi
          echo "merge_sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      - uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: android-deps-${{ runner.os }}-${{ hashFiles('gradle/**', 'gradlew*', 'gradle.properties', '*.gradle*') }}
          restore-keys: android-deps-${{ runner.os }}-

      - name: Configure Firebase and signing
        run: |
          cp androidApp/src/google-services-dummy.json androidApp/src/google-services.json
          echo "LOCAL_RELEASE_BUILD=true" > local.properties

      - name: Build debug APK (matches upstream CI)
        run: |
          ./gradlew :androidApp:assembleDebug
        env:
          GITHUB_ACTOR: ${{ secrets.READ_PACKAGES_ACTOR }}
          GITHUB_TOKEN: ${{ secrets.READ_PACKAGES_TOKEN }}

      - name: Locate APK
        id: apk
        run: |
          APK=$(ls androidApp/build/outputs/apk/debug/*.apk 2>/dev/null | head -1)
          if [ -z "$APK" ]; then
            echo "ERROR: No APK found in androidApp/build/outputs/apk/debug/"
            ls -la androidApp/build/outputs/apk/debug/ || true
            exit 1
          fi
          echo "path=$APK" >> "$GITHUB_OUTPUT"
          echo "name=$(basename "$APK")" >> "$GITHUB_OUTPUT"

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: nightly-${{ github.run_number }}
          name: Nightly build #${{ github.run_number }}
          body: |
            Automated APK build from [coredevices/mobileapp](https://github.com/coredevices/mobileapp).

            - Trigger: `${{ github.event_name }}`
            - Run: ${{ github.run_number }}
            - Commit: ${{ steps.merge.outputs.merge_sha }}
          files: ${{ steps.apk.outputs.path }}
          make_latest: true
          fail_on_unmatched_files: true
